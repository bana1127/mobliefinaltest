<?xml version="1.0" encoding="utf-8"?>
<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">
    <PreferenceCategory app:title="사진 설정">
        <ListPreference
            android:entries="@array/my_scale"
            android:entryValues="@array/my_scale_values"
            app:key="scale_type"
            app:title="Scale Type 선택"/>
    </PreferenceCategory>
    <PreferenceCategory app:title="배터리량 설정">
        <ListPreference
            android:entries="@array/my_color"
            android:entryValues="@array/my_color_values"
            app:key="s_color"
            app:title="색상 선택"/>
    </PreferenceCategory>
</PreferenceScreen>